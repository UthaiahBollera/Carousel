import * as React from "react";

interface Props {
  onChange: Function;
}

const Carousel: React.FC<Props> = ({ onChange }) => {
  return (
    <>
      <div></div>
    </>
  );
};
export default Carousel;
